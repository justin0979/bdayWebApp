'use strict'

// This object will store the user's birthday (or whatever date is entered).
// Can modify it to take names, addresses, etc. and store each object into
// a database (via MongoDB, or relationally via MySQL or however else) for
// future reference.

let person = {
	year: Number,
	month: Number,
	day: Number
}
